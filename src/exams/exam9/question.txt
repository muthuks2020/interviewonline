/**
 *
 * Try to complete the following ：
 *
 * const app = new DI()
 *
 * app.register('duck', {
 *   fly() {
 *     return 'duck flying'
 *   }
 * })
 *
 * app.run(['duck', function(duck){
 *   console.log(duck.fly()) //duck flying
 * }])
 *
 **/
class DI {

  register(name, instance) {
    //you code in here
  }

  run(arr) {
       //you code in here
  }
}
